fastapi==0.141.1
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0
SQLAlchemy==2.0.36
asyncpg==0.30.0
psycopg[binary]==3.2.3
PyJWT==2.13.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
cryptography==50.0.0
httpx==0.28.1
openai==3.6.0
croniter==6.0.0
python-multipart==0.0.32
email-validator==2.2.0
PyYAML==6.0.2
# Reads uploaded SQL for the Transform importer. dbt-core pulls this in
# too, but only when the image is built WITH_TRANSFORM=1 -- and the
# importer's behaviour must not drift with whatever version dbt happens
# to resolve, so it is pinned here on its own account. Pure Python, ~2 MB.
sqlglot==30.18.0
alembic==1.14.0
