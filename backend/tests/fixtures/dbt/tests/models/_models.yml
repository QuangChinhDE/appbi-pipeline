version: 2

models:
  - name: customers
    columns:
      - name: id
        data_tests:
          - not_null
          - unique
      - name: tier
        data_tests:
          - accepted_values:
              arguments:
                values: ['gold', 'silver', 'bronze']
