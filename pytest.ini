[pytest]
testpaths = backend/tests
python_files = test_*.py
# The integration suite needs a migrated, disposable database and opts in with
# RUN_TRANSFORM_INTEGRATION_TESTS=1; everything else runs offline.
addopts = -q
